---
title: 思维导图
summary: 全库可导航思维导图（自写 CSS + JS，无第三方依赖）——点击节点直达对应 wiki 页面。
tags: [viz, mindmap]
introduced-in: general
applies-to: [all]
status: stable
source: AGENTS.md
updated: 2026-07-09
---

# 思维导图（自写 CSS + JS，可导航）

下方为全库思维导图，**纯 CSS + 原生 JS 实现，不依赖任何第三方库/CDN**。
点击分类前的 `▸` 展开/收起；**点击叶子节点跳转到对应页面**。默认展开到一级分组。

<div id="mm" class="mm"></div>

<style>
  .mm { max-width: 1080px; margin: 12px 0 24px; font: 14px/1.5 var(--md-default-font-family, -apple-system, "Segoe UI", Roboto, "PingFang SC", "Microsoft YaHei", sans-serif); }
  .mm ul { list-style: none; margin: 0; padding-left: 20px; position: relative; }
  .mm > ul { padding-left: 0; }
  .mm li { position: relative; padding: 2px 0; }
  .mm ul ul::before { content: ""; position: absolute; left: 0; top: 0; bottom: 0; border-left: 2px solid #2a3140; }
  .mm li > .row::before { content: ""; position: absolute; left: -20px; top: 15px; width: 16px; border-top: 2px solid #2a3140; }
  .mm > ul > li > .row::before { display: none; }
  .mm .row { display: inline-flex; align-items: center; gap: 6px; }
  .mm .toggle { cursor: pointer; user-select: none; width: 14px; text-align: center; color: #8b97a7; font-size: 12px; transition: transform .15s ease, color .15s; }
  .mm .toggle:hover { color: #e6edf3; }
  .mm li > .row { border-radius: 6px; transition: background .15s; }
  .mm li > .row:hover { background: rgba(110,168,254,0.07); }
  .mm .node {
    display: inline-block; padding: 3px 10px; border-radius: 8px;
    background: #11161f; border: 1px solid #2a3140; color: #e6edf3;
    text-decoration: none; transition: border-color .15s, background .15s;
  }
  .mm .node:hover { border-color: #6ea8fe; background: #161d2b; }
  .mm .node.cat-root { border-color: #6ea8fe; font-weight: 600; background: #14233b; }
  .mm .node.cat-net  { border-left: 4px solid #6ea8fe; }
  .mm .node.cat-arch { border-left: 4px solid #4dd0c1; }
  .mm .node.cat-lang { border-left: 4px solid #79d17b; }
  .mm .node.cat-prac { border-left: 4px solid #c792ea; }
  .mm .node.cat-gov  { border-left: 4px solid #f78c6c; }
  .mm .node.leaf::after { content: " ↗"; color: #5b6675; font-size: 11px; }
  .mm .collapsed > ul { display: none; }
  .mm .collapsed > .row .toggle { transform: rotate(0deg); }
</style>

<script>
  // 全库结构（链接相对当前页：本页位于 /思维导图/，故统一加 ../ 前缀指向站点根下各页）
  var TREE = {
    t: ".NET LLM Wiki", c: "root", children: [
      { t: ".NET 平台", c: "net", children: [
        { t: ".NET 总览", h: "../dotnet/overview/" },
        { t: "C# 语言", c: "net", children: [
          { t: "C# 现代语言特性", h: "../dotnet/csharp/modern-csharp/" },
          { t: "C# 14 新特性", h: "../dotnet/csharp/csharp-14/" },
          { t: "异步编程", h: "../dotnet/csharp/async-await/" }
        ]},
        { t: "框架基础", c: "net", children: [
          { t: "依赖注入", h: "../dotnet/fundamentals/dependency-injection/" }
        ]},
        { t: "Web / Minimal API", c: "net", children: [
          { t: "ASP.NET Core 10", h: "../dotnet/aspnet-core/aspnet-core-10/" }
        ]},
        { t: "数据访问 EF Core", c: "net", children: [
          { t: "EF Core 10", h: "../dotnet/ef-core/ef-core-10/" },
          { t: "EF Core 数据访问", h: "../dotnet/ef-core/ef-data-access/" }
        ]},
        { t: "AOT 与部署", c: "net", children: [
          { t: "原生 AOT", h: "../dotnet/aot/native-aot/" },
          { t: "文件型应用", h: "../dotnet/aot/file-based-apps/" }
        ]},
        { t: "运行时", c: "net", children: [
          { t: "运行时 JIT 优化", h: "../dotnet/runtime/jit-optimizations/" }
        ]},
        { t: "Blazor", c: "net", children: [
          { t: "Blazor JS 改进", h: "../dotnet/blazor/javascript-improvements/" }
        ]},
        { t: "版本对照", c: "net", children: [
          { t: ".NET 8 / C# 12", h: "../dotnet/versions/net8/" },
          { t: ".NET 9 / C# 13", h: "../dotnet/versions/net9/" },
          { t: ".NET 版本演进", h: "../comparisons/net-evolution/" }
        ]}
      ]},
      { t: "架构", c: "arch", children: [
        { t: "模块化单体架构", h: "../architecture/modular-monolith/" }
      ]},
      { t: "工程规范", c: "lang", children: [
        { t: "命名与 API 约定", h: "../standards/coding-conventions/" },
        { t: "健壮性与工程质量", h: "../standards/quality-engineering/" }
      ]},
      { t: "实践", c: "prac", children: [
        { t: "模式", c: "prac", children: [
          { t: "组合与架构模式", h: "../patterns/composition/" },
          { t: "释放与 using", h: "../patterns/disposable-using/" }
        ]},
        { t: "反模式", c: "prac", children: [
          { t: "异步反模式", h: "../anti-patterns/async-antipatterns/" },
          { t: "设计与可维护性反模式", h: "../anti-patterns/design-antipatterns/" }
        ]},
        { t: "对比", c: "prac", children: [
          { t: "record vs class", h: "../comparisons/record-vs-class/" },
          { t: "List vs ImmutableArray", h: "../comparisons/list-vs-immutablearray/" },
          { t: "RAG vs LLM Wiki", h: "../comparisons/rag-vs-llm-wiki/" },
          { t: ".NET 版本演进", h: "../comparisons/net-evolution/" }
        ]}
      ]},
      { t: "治理", c: "gov", children: [
        { t: "质量准则 QA", h: "../governance/qa/" },
        { t: "持久约定 POLICY", h: "../governance/policy/" },
        { t: "质量报告", h: "../governance/qa-report/" },
        { t: "如何反馈", h: "../governance/feedback/" },
        { t: "资料索引", h: "../sources/" }
      ]}
    ]
  };

  function build(node, depth) {
    var li = document.createElement("li");
    if (depth >= 1) li.className = "collapsed";
    var row = document.createElement("div");
    row.className = "row";

    if (node.children && node.children.length) {
      var tog = document.createElement("span");
      tog.className = "toggle";
      tog.textContent = depth >= 1 ? "▸" : "▾";
      row.appendChild(tog);
      tog.addEventListener("click", function (e) {
        e.stopPropagation();
        var open = li.classList.toggle("collapsed");
        tog.textContent = open ? "▸" : "▾";
      });
    }

    var el = document.createElement(node.h ? "a" : "span");
    el.className = "node cat-" + (node.c || "root") + (node.h ? " leaf" : "");
    el.textContent = node.t;
    if (node.h) el.href = node.h;
    row.appendChild(el);

    li.appendChild(row);

    if (node.children && node.children.length) {
      var ul = document.createElement("ul");
      node.children.forEach(function (ch) { ul.appendChild(build(ch, depth + 1)); });
      li.appendChild(ul);
    }
    return li;
  }

  var root = document.getElementById("mm");
  var ul = document.createElement("ul");
  ul.appendChild(build(TREE, 0));
  root.appendChild(ul);
</script>

> 节点颜色按主题分组（蓝=.NET 平台、青=架构、绿=工程规范、紫=实践、橙=治理）。点击分类展开/收起，点击叶子节点在新位置打开对应页面。

<noscript>本思维导图需要启用 JavaScript 才能展开与跳转；也可直接通过左侧导航浏览全库。</noscript>
